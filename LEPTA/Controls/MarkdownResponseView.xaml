<UserControl x:Class="LEPTA.Controls.MarkdownResponseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="320"
             d:DesignWidth="420">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel x:Name="ToolbarPanel" Margin="0,0,0,8" LastChildFill="False">
            <Button x:Name="CopyResponseButton"
                    DockPanel.Dock="Right"
                    Content="Copy response"
                    Padding="10,4"
                    MinHeight="28"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="CopyResponseButton_Click" />
        </DockPanel>

        <Grid Grid.Row="1">
            <TextBox x:Name="StreamingTextBox"
                     Visibility="Collapsed"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="0"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled" />
            <ScrollViewer x:Name="RenderedScrollViewer"
                          Visibility="Collapsed"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="RenderedContentPanel" />
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>

